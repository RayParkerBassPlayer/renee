<h1><%="#{@user.name}'s Blog Posts!"%><h1> 

<ul>

<% @blogs.each do |blog| %>
<li><%= link_to blog.title, blog_path(blog, :user_id => @user.id) %></li>
<% end %>

</ul>

<%= link_to "Create New Blog", new_blog_path(:user_id => @user.id) %>